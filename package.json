{
  "name": "autoscape-classic",
  "version": "2.11.1",
  "private": true,
  "description": "Regression checks for the browser-only AutoScape build",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
